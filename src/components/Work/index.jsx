

import "./style.scss"

const index = () => {
    return (
        <section id="work" className="work">
            <div className="container">
                <h1>Work</h1>
            </div>
        </section>
    );
};

export default index;